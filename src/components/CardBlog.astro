---
interface Prop{
    image: string;
    title: string;
    description: string;
    link: string;
}

const {image, title, description, link} = Astro.props;
---

<div class="bg-white shadow-md rounded-lg overflow-hidden mb-6">
    <img src={image} alt={title} class="w-full h-56 object-cover" />
    <div class="p-4">
      <h3 class="text-xl font-medium mb-2">{title}</h3>
      <p class="text-gray-600 pb-5">{description}</p>
      <a
            href={link}
            class="bg-amber-400 hover:bg-amber-500 transition text-white text-base font-bold rounded-lg p-2"
          >
            Leer más
          </a>
    </div>
  </div>